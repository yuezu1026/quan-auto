# Python
__pycache__/
*.py[cod]

# 虚拟环境
.venv/
venv/

# 编辑器 / 系统
Thumbs.db
Desktop.ini

# ── 以下刻意**不**忽略，它们是证据快照，必须入库 ──────────────────
#   tools/gates-report.txt      上一次门禁的总判据（含棘轮数字）
#   tools/sql-smoke-report.txt  容器 PostgreSQL 冒烟证据（含镜像 digest）
#   tools/falsify-report.txt    逐条约束的证伪证据
#   tools/gates-baseline.json   棘轮基线（手工维护，没有 --write-baseline）
#   docs/*.docx                 3 份派生 md 的源文档
#   .vscode/tasks.json          门禁任务的唯一入口
# 如果将来要加忽略规则，先确认它不在上面这批里 —— 忽略掉证据 = 假绿。
