# 智能量化交易平台 — 核心模块接口契约文档 v1.0

## 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 技术栈 | Java（平台层）+ Python（研究层） |
| 通信协议 | gRPC / REST / Kafka |
| 目标读者 | AI Agent 开发引擎、架构师、后端工程师 |
| 约束级别 | 强约束 — 所有实现必须严格遵循本契约 |

## 全局约定

### 命名规范

- Python类名：PascalCase（如 `StrategyRegistry`）
- Python方法名：snake_case（如 `on_data`）
- 数据类：PascalCase（如 `MarketDataBundle`）
- 枚举值：UPPER_SNAKE_CASE（如 `BUY`、`SELL`）
- 接口名：PascalCase + “Service”/“Manager”后缀

### 异常处理规范

- 所有公共方法必须声明可能抛出的异常
- 使用自定义异常类层次：`BaseStrategyError` → 具体异常子类
- 异常信息必须包含 `strategy_id`、`symbol` 等上下文信息

### 日志规范

- 使用Python标准 `logging` 模块
- 日志级别：DEBUG（详细调试）、INFO（关键流程）、WARNING（可恢复异常）、ERROR（严重错误）、CRITICAL（系统级故障）
- 日志格式：`[%(levelname)s] [%(strategy_id)s] [%(symbol)s] %(message)s`
- 关键日志点：策略启动/停止、信号生成、订单提交、风控拦截、参数变更

## 模块一：策略中心（Strategy Center）

### 1.1 策略抽象接口规范

#### 1.1.1 Strategy 基类

```python
from abc import ABC, abstractmethod
from typing import Dict, List, Any, Optional
from datetime import datetime

class Strategy(ABC):
    """
    量化策略抽象基类。
    所有业务策略必须继承此基类并实现核心抽象方法。
    """

    @abstractmethod
    def __init__(self, strategy_id: str, config: Dict[str, Any]) -> None:
        """
        策略初始化方法。

        Args:
            strategy_id (str): 策略唯一标识，全局唯一。
            config (Dict[str, Any]): 策略配置参数字典。

        Raises:
            InvalidConfigError: 当配置参数缺失或类型不合法时抛出。
        """
        pass

    @abstractmethod
    def on_start(self) -> None:
        """
        策略启动回调。

        职责: 执行一次性初始化逻辑（如加载因子模型、建立数据源连接）。
        调用时机: 回测引擎启动时、实盘策略激活时。
        注意: 此方法必须是幂等的，可被多次安全调用。
        """
        pass

    @abstractmethod
    def on_data(self, data: MarketDataBundle) -> List[TradingSignal]:
        """
        核心数据处理方法。

        Args:
            data (MarketDataBundle): 市场数据Bundle，包含当前K线/Tick数据。

        Returns:
            List[TradingSignal]: 交易信号列表。返回空列表表示当前无交易意图。

        Raises:
            DataValidationError: 输入数据格式异常或缺失关键字段时抛出。
            StrategyExecutionError: 策略内部逻辑执行异常时抛出。

        注意: 必须是纯函数风格，不修改策略内部可变状态（状态变更通过返回值或框架注入传递）。
        """
        pass

    def on_bar(self, bar: BarData) -> None:
        """
        K线级别回调（可选覆盖）。

        Args:
            bar (BarData): 单根K线数据。

        职责: 每根K线完成时触发，用于K线级别的策略逻辑。
        默认实现: 将BarData转换为MarketDataBundle后调用 on_data。
        适用场景: 需要逐Bar处理的策略（如逐笔委托策略、高频Tick策略）。
        """
        pass

    @abstractmethod
    def on_order(self, order: Order) -> None:
        """
        订单状态更新回调。

        Args:
            order (Order): 订单对象，包含最新的订单状态信息。

        职责: 订单状态变更时触发（已提交、部分成交、完全成交、已撤销、已拒绝）。
        注意: 用于更新策略内部订单跟踪状态，不应在此方法中发起新的下单请求。
        """
        pass

    @abstractmethod
    def on_trade(self, trade: Trade) -> None:
        """
        成交回报回调。

        Args:
            trade (Trade): 成交记录对象。

        职责: 每笔成交发生时触发，用于记录成交明细和实时计算盈亏。
        """
        pass

    @abstractmethod
    def on_stop(self) -> None:
        """
        策略停止回调。

        职责: 执行清理逻辑（如释放资源、保存持久化状态、断开连接）。
        调用时机: 回测引擎停止时、实盘策略停用/退役时。
        """
        pass

    @abstractmethod
    def get_strategy_info(self) -> StrategyInfo:
        """
        返回策略元信息。

        Returns:
            StrategyInfo: 包含策略ID、名称、版本、描述、作者、创建时间等信息的对象。
        """
        pass

    @abstractmethod
    def get_target_positions(self) -> Dict[str, PositionTarget]:
        """
        返回目标仓位快照。

        Returns:
            Dict[str, PositionTarget]: 字典，key为股票代码，value为PositionTarget对象
                                       （含目标数量、目标价格、优先级）。

        职责: 供组合调度层使用，返回的是"持仓意图"而非直接下单指令。
        """
        pass

    @abstractmethod
    def get_strategy_params(self) -> Dict[str, Any]:
        """
        返回当前参数配置。

        Returns:
            Dict[str, Any]: 当前参数配置的深拷贝字典，防止外部意外修改内部状态。
        """
        pass

    @abstractmethod
    def update_params(self, params: Dict[str, Any]) -> bool:
        """
        动态更新策略参数。

        Args:
            params (Dict[str, Any]): 要更新的参数字典。

        Returns:
            bool: 成功返回True，失败返回False。

        Raises:
            InvalidParamError: 参数不合法或超出允许范围时抛出。

        职责: 运行时动态更新策略参数，无需重启策略实例。
        """
        pass

    def on_error(self, error: StrategyError) -> None:
        """
        错误处理回调。

        Args:
            error (StrategyError): 策略执行过程中发生的错误对象。

        职责: 供策略实现自定义错误处理逻辑（如告警、降级、熔断）。
        默认实现: 记录ERROR级别日志。
        """
        pass
```

#### 1.1.2 MarketDataBundle 数据类

```python
from dataclasses import dataclass
from typing import Optional
from datetime import datetime

@dataclass
class MarketDataBundle:
    """
    市场数据聚合包，包含单只股票在特定时间点的完整行情信息。
    """
    symbol: str               # 股票代码（如 '600519.SH'）
    open: float               # 开盘价
    high: float               # 最高价
    low: float                # 最低价
    close: float              # 收盘价
    volume: int               # 成交量（单位：股）
    amount: float             # 成交额（单位：元）
    datetime: datetime        # K线时间戳（本地时间）
    timestamp: int            # Unix时间戳（毫秒级）
    adjust_factor: float      # 复权因子（用于前/后复权价格计算）
    previous_close: float     # 前收盘价（用于涨跌幅计算基准）
    turn_rate: Optional[float] = None   # 换手率（百分比），非交易时段可能为None
    limit_up: Optional[float] = None    # 涨停价，非交易时段可能为None
    limit_down: Optional[float] = None  # 跌停价，非交易时段可能为None
    is_trading_day: bool = True         # 当前是否为交易日
```

#### 1.1.3 BarData 数据类

```python
@dataclass
class BarData:
    """
    轻量级K线数据类，适用于逐Bar回调场景。
    """
    symbol: str          # 股票代码
    open: float          # 开盘价
    high: float          # 最高价
    low: float           # 最低价
    close: float         # 收盘价
    volume: int          # 成交量（股）
    amount: float        # 成交额（元）
    datetime: datetime   # K线时间戳
    timestamp: int       # Unix时间戳（毫秒）
```

#### 1.1.4 TradingSignal 数据类

```python
import uuid
from dataclasses import dataclass, field
from typing import Dict, Any, Optional
from datetime import datetime

@dataclass
class TradingSignal:
    """
    交易信号数据类，由策略生成并传递给订单路由层。
    """
    signal_id: str = field(default_factory=lambda: str(uuid.uuid4()))  # 信号唯一标识（UUID）
    strategy_id: str = ""                # 生成该信号的策略ID
    symbol: str = ""                     # 目标股票代码
    direction: DirectionEnum = DirectionEnum.BUY  # 交易方向
    signal_type: SignalTypeEnum = SignalTypeEnum.OPEN  # 信号类型
    target_quantity: int = 0             # 目标数量（股），必须为正整数
    target_price: Optional[float] = None # 目标价格，None表示市价单
    priority: int = 5                    # 优先级（1最高，10最低），用于组合调度排序
    timestamp: datetime = field(default_factory=datetime.now)  # 信号生成时间
    reason: str = ""                     # 信号生成原因/逻辑描述（用于审计和回溯）
    metadata: Dict[str, Any] = field(default_factory=dict)  # 附加元数据（因子值、置信度等）
```

#### 1.1.5 StrategyInfo 数据类

```python
@dataclass
class StrategyInfo:
    """
    策略元信息数据类。
    """
    strategy_id: str                     # 策略唯一标识
    strategy_name: str                   # 策略名称
    version: str                         # 版本号（语义化版本，如 '1.0.0'）
    description: str                     # 策略描述
    author: str                          # 作者
    created_at: datetime                 # 创建时间
    updated_at: datetime                 # 最后更新时间
    status: StrategyStatusEnum = StrategyStatusEnum.IDLE  # 当前状态
    params_schema: Dict[str, Any] = field(default_factory=dict)  # 参数Schema定义（JSON Schema格式）
```

#### 1.1.6 DirectionEnum 和 SignalTypeEnum 枚举

```python
from enum import Enum

class DirectionEnum(Enum):
    """交易方向枚举"""
    BUY = "BUY"       # 买入
    SELL = "SELL"     # 卖出

class SignalTypeEnum(Enum):
    """信号类型枚举"""
    OPEN = "OPEN"         # 开仓
    CLOSE = "CLOSE"       # 平仓
    ADJUST = "ADJUST"     # 调仓

class StrategyStatusEnum(Enum):
    """策略状态枚举"""
    IDLE = "IDLE"         # 空闲/未启动
    RUNNING = "RUNNING"   # 运行中
    STOPPED = "STOPPED"   # 已停止
    PAUSED = "PAUSED"     # 已暂停
```

### 1.2 策略注册与管理接口

#### 1.2.1 StrategyRegistry 类

```python
class StrategyRegistry:
    """
    策略注册与管理中心。
    负责策略实例的生命周期管理、状态追踪和统一调度。
    """

    def register(self, strategy: Strategy) -> bool:
        """
        注册策略实例。

        Args:
            strategy (Strategy): 策略实例。

        Returns:
            bool: 注册成功返回True。

        Raises:
            DuplicateStrategyError: 策略ID已存在时抛出。
        """
        pass

    def unregister(self, strategy_id: str) -> bool:
        """
        注销策略。

        Args:
            strategy_id (str): 策略唯一标识。

        Returns:
            bool: 注销成功返回True。

        Raises:
            StrategyNotFoundError: 策略ID不存在时抛出。
            IllegalStateError: 策略处于RUNNING状态时不允许注销。
        """
        pass

    def get_strategy(self, strategy_id: str) -> Strategy:
        """
        获取策略实例。

        Args:
            strategy_id (str): 策略唯一标识。

        Returns:
            Strategy: 策略实例。

        Raises:
            StrategyNotFoundError: 策略ID不存在时抛出。
        """
        pass

    def list_strategies(self) -> List[StrategyInfo]:
        """
        列出所有已注册策略的元信息。

        Returns:
            List[StrategyInfo]: 策略元信息列表。
        """
        pass

    def start_all(self) -> None:
        """
        启动所有已注册且处于IDLE/STOPPED状态的策略。
        依次调用每个策略的 on_start() 方法。
        """
        pass

    def stop_all(self) -> None:
        """
        停止所有运行中的策略。
        依次调用每个策略的 on_stop() 方法。
        """
        pass

    def pause_strategy(self, strategy_id: str) -> bool:
        """
        暂停指定策略。

        Args:
            strategy_id (str): 策略唯一标识。

        Returns:
            bool: 暂停成功返回True。

        Raises:
            StrategyNotFoundError: 策略不存在。
            IllegalStateError: 策略不在RUNNING状态。
        """
        pass

    def resume_strategy(self, strategy_id: str) -> bool:
        """
        恢复暂停的策略。

        Args:
            strategy_id (str): 策略唯一标识。

        Returns:
            bool: 恢复成功返回True。

        Raises:
            StrategyNotFoundError: 策略不存在。
            IllegalStateError: 策略不在PAUSED状态。
        """
        pass

    def get_strategy_status(self, strategy_id: str) -> StrategyStatusEnum:
        """
        获取策略当前状态。

        Args:
            strategy_id (str): 策略唯一标识。

        Returns:
            StrategyStatusEnum: 策略当前状态枚举值。

        Raises:
            StrategyNotFoundError: 策略不存在。
        """
        pass
```

### 1.3 策略-股票适配引擎接口

#### 1.3.1 StrategyStockAdapter 类

```python
class StrategyStockAdapter:
    """
    策略-股票适配引擎。
    基于市场状态和策略特性，动态计算策略与标的的适配度，生成最优股票池。
    """

    def calculate_adaptability(self, strategy_id: str, symbol: str, market_state: MarketState) -> AdaptabilityScore:
        """
        计算指定策略与标的在当前市场状态下的适配度。

        Args:
            strategy_id (str): 策略唯一标识。
            symbol (str): 股票代码。
            market_state (MarketState): 当前市场状态。

        Returns:
            AdaptabilityScore: 适配度评分结果。

        Raises:
            AdaptabilityCalculationError: 计算过程异常时抛出。
        """
        pass

    def get_strategy_pool(self, strategy_id: str, top_n: int = 50) -> List[SymbolScore]:
        """
        获取指定策略的最优股票池。

        Args:
            strategy_id (str): 策略唯一标识。
            top_n (int): 返回前N只股票，默认50。

        Returns:
            List[SymbolScore]: 按适配度降序排列的股票评分列表。
        """
        pass

    def update_adaptability_scores(self, strategy_id: str, symbols: List[str], market_state: MarketState) -> None:
        """
        批量更新适配度评分。

        Args:
            strategy_id (str): 策略唯一标识。
            symbols (List[str]): 股票代码列表。
            market_state (MarketState): 当前市场状态。
        """
        pass

    def get_market_state(self) -> MarketState:
        """
        获取当前市场状态快照。

        Returns:
            MarketState: 市场状态数据对象。

        Raises:
            MarketStateFetchError: 获取失败时抛出。
        """
        pass

    def get_available_strategies(self) -> List[str]:
        """
        获取当前可用的策略类型列表。

        Returns:
            List[str]: 策略类型标识列表。
        """
        pass

    def add_adaptability_rule(self, rule: AdaptabilityRule) -> bool:
        """
        添加适配度计算规则。

        Args:
            rule (AdaptabilityRule): 适配规则对象。

        Returns:
            bool: 添加成功返回True。
        """
        pass

    def remove_adaptability_rule(self, rule_id: str) -> bool:
        """
        移除适配度计算规则。

        Args:
            rule_id (str): 规则唯一标识。

        Returns:
            bool: 移除成功返回True。
        """
        pass
```

#### 1.3.2 MarketState 数据类

```python
@dataclass
class MarketState:
    """
    市场状态数据类，由适配引擎或宏观分析模块生成。
    """
    trend_probability: float      # 趋势市概率（0-1）
    oscillation_probability: float  # 震荡市概率（0-1）
    bull_probability: float       # 牛市概率（0-1）
    bear_probability: float       # 熊市概率（0-1）
    large_cap_index: float        # 大盘风格指数（0-1）
    small_cap_index: float        # 小盘风格指数（0-1）
    value_index: float            # 价值风格指数（0-1）
    growth_index: float           # 成长风格指数（0-1）
    liquidity_score: float        # 流动性评分（0-100）
    volatility_index: float       # 波动率指数
    volume_trend: str             # 成交量趋势：EXPANDING / CONDENSING / NEUTRAL
    timestamp: datetime           # 状态计算时间戳
```

#### 1.3.3 AdaptabilityScore 数据类

```python
@dataclass
class AdaptabilityScore:
    """
    适配度评分结果数据类。
    """
    score: float                           # 适配度分数（0-100）
    confidence: float                      # 置信度（0-1）
    factor_breakdown: Dict[str, float]     # 各因子贡献度分解
    symbol: str                            # 股票代码
    strategy_id: str                       # 策略ID
    timestamp: datetime                    # 评分时间戳
    version: str                           # 评分模型版本号
```

#### 1.3.4 SymbolScore 数据类

```python
@dataclass
class SymbolScore:
    """
    符号评分数据类，用于股票池排序。
    """
    symbol: str          # 股票代码
    score: float         # 适配度分数
    rank: int            # 排名（1-based）
    strategy_id: str     # 策略ID
```

### 1.4 gRPC接口定义（Proto文件）

#### 1.4.1 公共消息定义（common.proto）

```protobuf
syntax = "proto3"; package quant.strategy.common;
import "google/protobuf/timestamp.proto";
// 策略ID请求 message StrategyIdRequest { string strategy_id = 1; // 策略唯一标识 }
// 策略ID响应 message StrategyIdResponse { bool success = 1; // 操作是否成功 string message = 2; // 附加消息 }
// 策略信息响应 message StrategyInfoResponse { string strategy_id = 1; string strategy_name = 2; string version = 3; string description = 4; string author = 5; google.protobuf.Timestamp created_at = 6; google.protobuf.Timestamp updated_at = 7; string status = 8; // StrategyStatusEnum值 string params_schema = 9; // JSON字符串 }
// 市场数据请求 message MarketDataRequest { string symbol = 1; // 股票代码 string frequency = 2; // 频率：1m/5m/15m/1h/1d google.protobuf.Timestamp start_time = 3; google.protobuf.Timestamp end_time = 4; int32 limit = 5; // 返回条数限制 }
// 市场数据响应 message MarketDataResponse { string symbol = 1; double open = 2; double high = 3; double low = 4; double close = 5; int64 volume = 6; double amount = 7; google.protobuf.Timestamp datetime = 8; int64 timestamp = 9; // Unix毫秒时间戳 double adjust_factor = 1
```

# 智能量化交易平台 — 核心模块接口契约文档 v1.0（第二部分：回测引擎）

## 模块二：回测引擎（Backtesting Engine）

### 2.1 回测引擎核心类

#### 2.1.1 BacktestEngine 类

回测引擎核心类，负责协调数据源、策略、撮合引擎及绩效分析器，执行完整的回测流程。

- `__init__(self, config: BacktestConfig) -> None`
  - **参数**：`config` (BacktestConfig) — 回测配置对象。
  - **返回值**：无。
  - **职责**：初始化引擎，加载配置，初始化事件引擎、数据源管理器、撮合引擎、绩效分析器。
  - **异常**：`ConfigValidationError` — 当配置验证失败时抛出。
- `add_datafeed(self, symbol: str, datafeed: DataFeed) -> bool`
  - **参数**：`symbol` (str) — 股票代码；`datafeed` (DataFeed) — 数据源实现。
  - **返回值**：`bool` — 注册成功返回 True，失败返回 False。
  - **职责**：为指定股票注册数据源。
  - **异常**：`DataFeedRegistrationError` — 数据源注册失败时抛出。
- `add_strategy(self, strategy: Strategy, capital: float, params: Dict[str, Any]) -> StrategyHandle`
  - **参数**：`strategy` (Strategy) — 策略实例；`capital` (float) — 初始资金；`params` (Dict[str, Any]) — 策略参数。
  - **返回值**：`StrategyHandle` — 策略句柄（含策略ID和初始资金）。
  - **职责**：注册策略到回测引擎，分配初始资金。
  - **异常**：`StrategyRegistrationError` — 策略注册失败时抛出。
- `run(self) -> BacktestResult`
  - **参数**：无。
  - **返回值**：`BacktestResult` — 回测结果对象。
  - **职责**：启动回测引擎，遍历所有数据，执行策略逻辑，返回完整回测结果。
  - **异常**：`BacktestExecutionError` — 回测执行过程中发生错误时抛出。
- `run_partial(self, start_date: str, end_date: str) -> BacktestResult`
  - **参数**：`start_date` (str) — 起始日期（YYYY-MM-DD）；`end_date` (str) — 结束日期（YYYY-MM-DD）。
  - **返回值**：`BacktestResult` — 回测结果对象。
  - **职责**：在指定日期范围内执行回测。
  - **异常**：`BacktestExecutionError` — 回测执行过程中发生错误时抛出。
- `stop(self) -> None`
  - **参数**：无。
  - **返回值**：无。
  - **职责**：安全停止正在运行的回测引擎。
  - **异常**：无。
- `get_result(self) -> BacktestResult`
  - **参数**：无。
  - **返回值**：`BacktestResult` — 最近一次回测的结果对象。
  - **职责**：获取最近一次回测的结果。
  - **异常**：`NoResultError` — 尚未执行回测时抛出。
- `export_report(self, path: str) -> bool`
  - **参数**：`path` (str) — 报告保存路径。
  - **返回值**：`bool` — 导出成功返回 True。
  - **职责**：导出回测报告至指定路径。
  - **异常**：`ExportError` — 导出失败时抛出。
- `validate_no_leakage(self) -> ValidationReport`
  - **参数**：无。
  - **返回值**：`ValidationReport` — 验证报告。
  - **职责**：检查策略是否存在未来函数泄露。
  - **异常**：无。
- `checkpoint(self, path: str) -> bool`
  - **参数**：`path` (str) — 检查点保存路径。
  - **返回值**：`bool` — 保存成功返回 True。
  - **职责**：保存当前回测状态，支持断点续跑。
  - **异常**：`CheckpointError` — 检查点保存失败时抛出。
- `restore_checkpoint(self, path: str) -> bool`
  - **参数**：`path` (str) — 检查点路径。
  - **返回值**：`bool` — 恢复成功返回 True。
  - **职责**：从检查点恢复回测状态。
  - **异常**：`CheckpointError` — 检查点恢复失败时抛出。

#### 2.1.2 BacktestConfig 数据类

回测配置数据类，定义回测运行所需的所有参数。

```python
@dataclass
class BacktestConfig:
    strategy_configs: List[StrategyConfig]        # 策略配置列表
    datafeeds: Dict[str, DataFeed]                # 数据源字典（key为股票代码）
    initial_capital: float                        # 初始资金
    start_date: datetime                          # 回测起始日期
    end_date: datetime                            # 回测结束日期
    commission_config: CommissionConfig           # 佣金配置
    slippage_config: SlippageConfig               # 滑点配置
    benchmark: str                                # 基准指数代码
    benchmark_datafeed: Optional[DataFeed]        # 基准数据源
    output_path: str                              # 输出路径
    enable_checkpoint: bool                       # 是否启用检查点
    checkpoint_interval: int                      # 检查点间隔（Bar数）
    enable_parallel: bool                         # 是否启用并行回测
    max_workers: int                              # 并行工作线程数
    risk_free_rate: float                         # 无风险利率
    tax_config: TaxConfig                         # 税费配置
    position_limit: PositionLimit                 # 持仓限制配置
    capital_allocation: CapitalAllocation         # 资金分配策略
```

#### 2.1.3 BacktestResult 数据类

回测结果数据类，包含完整的回测输出信息。

```python
@dataclass
class BacktestResult:
    strategy_id: str                              # 策略ID
    status: BacktestStatus                        # 回测状态（SUCCESS/FAILED/PARTIAL/STOPPED）
    start_time: datetime                          # 回测开始时间
    end_time: datetime                            # 回测结束时间
    duration_ms: int                              # 回测耗时（毫秒）
    performance: PerformanceMetrics               # 绩效指标
    equity_curve: List[EquityPoint]               # 权益曲线数据点
    trades: List[Trade]                           # 成交记录
    orders: List[Order]                           # 订单记录
    account_history: List[AccountSnapshot]        # 账户快照历史
    error_message: Optional[str]                  # 错误信息
    data_version: str                             # 数据版本
    strategy_version: str                         # 策略版本
    params_used: Dict[str, Any]                   # 使用的参数
    validation_report: Optional[ValidationReport] # 验证报告
```

### 2.2 数据源抽象接口

#### 2.2.1 DataFeed 抽象类

数据源抽象基类，所有数据源实现必须继承此类。

```python
from abc import ABC, abstractmethod
from typing import Optional, List
from datetime import datetime

class DataFeed(ABC):
    @abstractmethod
    def get_bar(self, symbol: str, datetime: datetime) -> Optional[BarData]:
        """
        获取单根K线
        参数：symbol (str) — 股票代码；datetime (datetime) — 时间戳
        返回值：Optional[BarData] — K线数据，不存在返回None
        异常：DataFeedError — 数据获取失败
        """
        pass

    @abstractmethod
    def get_bars(self, symbol: str, start: datetime, end: datetime) -> List[BarData]:
        """
        批量获取K线
        参数：symbol (str) — 股票代码；start (datetime) — 起始时间；end (datetime) — 结束时间
        返回值：List[BarData] — K线数据列表
        异常：DataFeedError — 数据获取失败
        """
        pass

    @abstractmethod
    def get_available_symbols(self) -> List[str]:
        """
        获取可用股票列表
        返回值：List[str] — 股票代码列表
        异常：DataFeedError — 获取失败
        """
        pass

    @abstractmethod
    def get_available_dates(self, symbol: str) -> List[datetime]:
        """
        获取可用日期列表
        参数：symbol (str) — 股票代码
        返回值：List[datetime] — 可用日期列表
        异常：DataFeedError — 获取失败
        """
        pass

    @abstractmethod
    def is_symbol_available(self, symbol: str, datetime: datetime) -> bool:
        """
        检查股票在指定日期是否可交易（考虑停牌、涨跌停）
        参数：symbol (str) — 股票代码；datetime (datetime) — 时间戳
        返回值：bool — 可交易返回True
        异常：DataFeedError — 检查失败
        """
        pass

    @abstractmethod
    def get_adjustment_factor(self, symbol: str, datetime: datetime) -> float:
        """
        获取复权因子
        参数：symbol (str) — 股票代码；datetime (datetime) — 时间戳
        返回值：float — 复权因子
        异常：DataFeedError — 获取失败
        """
        pass

    @abstractmethod
    def get_dividend(self, symbol: str, datetime: datetime) -> float:
        """
        获取分红数据
        参数：symbol (str) — 股票代码；datetime (datetime) — 时间戳
        返回值：float — 分红金额
        异常：DataFeedError — 获取失败
        """
        pass

    @abstractmethod
    def get_trading_calendar(self, start: datetime, end: datetime) -> List[datetime]:
        """
        获取交易日历
        参数：start (datetime) — 起始日期；end (datetime) — 结束日期
        返回值：List[datetime] — 交易日列表
        异常：DataFeedError — 获取失败
        """
        pass

    @abstractmethod
    def get_market_status(self, datetime: datetime) -> MarketStatus:
        """
        获取市场状态（开盘/收盘/停牌）
        参数：datetime (datetime) — 时间戳
        返回值：MarketStatus — 市场状态枚举
        异常：DataFeedError — 获取失败
        """
        pass
```

#### 2.2.2 数据源实现示例

CSV数据源实现类示例：

```python
import pandas as pd
from typing import Optional, List
from datetime import datetime

class CsvDataFeed(DataFeed):
    def __init__(self, csv_path: str):
        self.csv_path = csv_path
        self._data: Dict[str, pd.DataFrame] = {}
        self._load_data()

    def _load_data(self):
        df = pd.read_csv(self.csv_path, parse_dates=['datetime'])
        for symbol, group in df.groupby('symbol'):
            self._data[symbol] = group.set_index('datetime')

    def get_bar(self, symbol: str, datetime: datetime) -> Optional[BarData]:
        if symbol not in self._data:
            return None
        df = self._data[symbol]
        if datetime not in df.index:
            return None
        row = df.loc[datetime]
        return BarData(
            symbol=symbol,
            datetime=datetime,
            open=row['open'],
            high=row['high'],
            low=row['low'],
            close=row['close'],
            volume=row['volume']
        )

    def get_bars(self, symbol: str, start: datetime, end: datetime) -> List[BarData]:
        if symbol not in self._data:
            return []
        df = self._data[symbol]
        mask = (df.index >= start) & (df.index <= end)
        filtered = df[mask]
        return [
            BarData(
                symbol=symbol,
                datetime=row.name,
                open=row['open'],
                high=row['high'],
                low=row['low'],
                close=row['close'],
                volume=row['volume']
            )
            for _, row in filtered.iterrows()
        ]

    def get_available_symbols(self) -> List[str]:
        return list(self._data.keys())

    def get_available_dates(self, symbol: str) -> List[datetime]:
        if symbol not in self._data:
            return []
        return self._data[symbol].index.tolist()

    def is_symbol_available(self, symbol: str, datetime: datetime) -> bool:
        bar = self.get_bar(symbol, datetime)
        return bar is not None and bar.volume > 0

    def get_adjustment_factor(self, symbol: str, datetime: datetime) -> float:
        # CSV中需包含adj_factor列
        if symbol not in self._data:
            return 1.0
        df = self._data[symbol]
        if datetime not in df.index or 'adj_factor' not in df.columns:
            return 1.0
        return float(df.loc[datetime, 'adj_factor'])

    def get_dividend(self, symbol: str, datetime: datetime) -> float:
        if symbol not in self._data:
            return 0.0
        df = self._data[symbol]
        if datetime not in df.index or 'dividend' not in df.columns:
            return 0.0
        return float(df.loc[datetime, 'dividend'])

    def get_trading_calendar(self, start: datetime, end: datetime) -> List[datetime]:
        all_dates = set()
        for df in self._data.values():
            mask = (df.index >= start) & (df.index <= end)
            all_dates.update(df[mask].index.tolist())
        return sorted(list(all_dates))

    def get_market_status(self, datetime: datetime) -> MarketStatus:
        for symbol, df in self._data.items():
            if datetime in df.index:
                return MarketStatus.OPEN
        return MarketStatus.CLOSED
```

### 2.3 事件引擎

#### 2.3.1 EventEngine 类

事件驱动引擎类，负责事件的发布与订阅。

```python
class EventEngine:
    def subscribe(self, event_type: EventType, handler: Callable) -> str:
        """
        订阅事件
        参数：event_type (EventType) — 事件类型；handler (Callable) — 处理函数
        返回值：str — 订阅ID
        """
        pass

    def unsubscribe(self, subscription_id: str) -> bool:
        """
        取消订阅
        参数：subscription_id (str) — 订阅ID
        返回值：bool — 取消成功返回True
        """
        pass

    def publish(self, event: Event) -> bool:
        """
        发布事件
        参数：event (Event) — 事件对象
        返回值：bool — 发布成功返回True
        """
        pass

    def start(self) -> None:
        """启动事件引擎"""
        pass

    def stop(self) -> None:
        """停止事件引擎"""
        pass

    def run(self) -> None:
        """事件循环主函数"""
        pass

    def get_queue_size(self) -> int:
        """
        获取队列大小
        返回值：int — 当前队列中的事件数量
        """
        pass

    def clear(self) -> None:
        """清空事件队列"""
        pass
```

#### 2.3.2 EventType 枚举

```python
from enum import Enum

class EventType(Enum):
    BAR_EVENT = "bar"
    TICK_EVENT = "tick"
    ORDER_EVENT = "order"
    TRADE_EVENT = "trade"
    ACCOUNT_EVENT = "account"
    POSITION_EVENT = "position"
    STRATEGY_EVENT = "strategy"
    RISK_EVENT = "risk"
    SYSTEM_EVENT = "system"
```

#### 2.3.3 Event 数据类

```python
@dataclass
class Event:
    event_type: EventType       # 事件类型
    data: Any                   # 事件数据
    timestamp: datetime         # 事件时间戳
    source: str                 # 事件来源
    priority: int               # 优先级（数值越小优先级越高）
```

### 2.4 模拟撮合引擎

#### 2.4.1 SimulatedBroker 类

模拟经纪商/撮合引擎类，负责订单撮合与账户管理。

```python
class SimulatedBroker:
    def submit_order(self, order: Order) -> OrderId:
        """
        提交订单
        参数：order (Order) — 订单对象
        返回值：OrderId — 订单ID
        """
        pass

    def cancel_order(self, order_id: OrderId) -> bool:
        """
        撤销订单
        参数：order_id (OrderId) — 订单ID
        返回值：bool — 撤销成功返回True
        """
        pass

    def get_account(self) -> Account:
        """
        获取账户信息
        返回值：Account — 账户对象
        """
        pass

    def get_positions(self) -> Dict[str, Position]:
        """
        获取持仓
        返回值：Dict[str, Position] — 持仓字典（key为股票代码）
        """
        pass

    def get_orders(self, strategy_id: str) -> List[Order]:
        """
        获取订单列表
        参数：strategy_id (str) — 策略ID
        返回值：List[Order] — 订单列表
        """
        pass

    def get_trades(self, strategy_id: str) -> List[Trade]:
        """
        获取成交记录
        参数：strategy_id (str) — 策略ID
        返回值：List[Trade] — 成交记录列表
        """
        pass

    def on_bar(self, bar: BarData) -> None:
        """
        K线回调处理撮合
        参数：bar (BarData) — K线数据
        """
        pass

    def set_commission(self, commission: CommissionConfig) -> None:
        """
        设置佣金配置
        参数：commission (CommissionConfig) — 佣金配置
        """
        pass

    def set_slippage(self, slippage: SlippageConfig) -> None:
        """
        设置滑点配置
        参数：slippage (SlippageConfig) — 滑点配置
        """
        pass

    def get_fills(self) -> List[Fill]:
        """
        获取成交明细
        返回值：List[Fill] — 成交明细列表
        """
        pass

    def reset(self) -> None:
        """重置经纪商状态"""
        pass
```

#### 2.4.2 Order 数据类

```python
@dataclass
class Order:
    order_id: str                   # 订单ID
    strategy_id: str                # 策略ID
    symbol: str                     # 股票代码
    direction: Direction            # 方向（BUY/SELL）
    order_type: OrderType           # 订单类型（LIMIT/MARKET/STOP/STOP_LIMIT）
    quantity: int                   # 委托数量
    price: Optional[float]          # 委托价格（市价单为None）
    stop_price: Optional[float]     # 止损价格
    status: OrderStatus             # 订单状态（PENDING/SUBMITTED/PARTIAL/FILLED/CANCELLED/REJECTED/EXPIRED）
    submit_time: datetime           # 提交时间
    last_update_time: datetime      # 最后更新时间
    filled_quantity: int            # 已成交数量
    filled_price: float             # 成交均价
    commission: float               # 手续费
    slippage: float                 # 滑点损失
    error_message: Optional[str]    # 错误信息
```

#### 2.4.3 Trade 数据类

```python
@dataclass
class Trade:
    trade_id: str           # 成交ID
    order_id: str           # 关联订单ID
    symbol: str             # 股票代码
    direction: Direction    # 方向
    price: float            # 成交价格
    quantity: int           # 成交数量
    commission: float       # 手续费
    slippage: float         # 滑点损失
    timestamp: datetime     # 成交时间
    trade_fee: float        # 交易费用合计
```

#### 2.4.4 Position 数据类

```python
@dataclass
class Position:
    symbol: str                     # 股票代码
    direction: PositionDirection    # 持仓方向（LONG/SHORT）
    quantity: int                   # 持仓数量
    market_value: float             # 市值
    cost_price: float               # 成本价
    current_price: float            # 当前价
    unrealized_pnl: float           # 未实现盈亏
    realized_pnl: float             # 已实现盈亏
    pnl_percentage: float           # 盈亏百分比
```

#### 2.4.5 Account 数据类

```python
@dataclass class Account: account_id: str # 账户ID total_capital: float # 总资产 available_capital: float # 可用资金 market_value: float # 持仓市值 frozen_capital: float # 冻结资金 total_pnl: float # 总盈亏
```

#### 2.4.6 CommissionConfig 数据类

定义佣金配置数据类，使用Python dataclass语法定义，包含以下字段：

```python
@dataclass
class CommissionConfig:
    """
    佣金配置数据类，用于定义回测模拟撮合过程中的各项交易费用。
    """
    stock_commission_rate: float = 0.0003
    """
    float: 股票佣金费率。
    默认值为 0.0003（即万分之三），表示按成交金额的万分之三收取佣金。
    """

    min_commission: float = 5.0
    """
    float: 最低佣金。
    默认值为 5.0 元。当单笔交易计算出的佣金不足此值时，按此最低值收取。
    """

    stamp_tax_rate: float = 0.001
    """
    float: 印花税费率。
    默认值为 0.001（即千分之一），仅卖出时收取。
    """

    transfer_fee: float = 0.00001
    """
    float: 过户费率。
    默认值为 0.00001（即十万分之一），买卖双向收取。
    """

    handling_fee: float = 0.0000487
    """
    float: 经手费率。
    默认值为 0.0000487，买卖双向收取。
    """

    stamp_tax_on_sell_only: bool = True
    """
    bool: 印花税是否仅卖出收取。
    默认值为 True，遵循A股交易规则，仅在卖出时收取印花税。
    """
```

#### 2.4.7 SlippageConfig 数据类

定义滑点配置数据类，使用Python dataclass语法定义，包含以下字段：

```python
@dataclass
class SlippageConfig:
    """
    滑点配置数据类，用于模拟真实市场中的价格冲击和交易摩擦。
    """
    fixed_slippage: float = 0.0
    """
    float: 固定滑点（元）。
    每笔交易产生的固定滑点损失，与交易金额无关。
    """

    percentage_slippage: float = 0.0
    """
    float: 比例滑点。
    按成交价比例计算的滑点，例如 0.001 表示 0.1% 的滑点。
    """

    volume_impact_factor: float = 0.0
    """
    float: 成交量冲击因子。
    大单对市场价格的冲击系数，通常与订单量占市场成交量的比例相关。
    """

    min_slippage: float = 0.0
    """
    float: 最小滑点（元）。
    滑点计算的下限，确保滑点不会低于此值。
    """

    max_slippage: float = float('inf')
    """
    float: 最大滑点（元）。
    滑点计算的上限，防止极端情况下滑点过大导致回测失真。
    """
```

### 2.5 绩效分析模块

#### 2.5.1 PerformanceAnalyzer 类

定义绩效分析类，包含以下方法：

```python
class PerformanceAnalyzer:
    """
    绩效分析器，负责计算回测结果的各项绩效指标。
    """

    def analyze(
        self,
        trades: List[Trade],
        orders: List[Order],
        account_history: List[AccountSnapshot]
    ) -> BacktestResult:
        """
        综合分析，整合所有绩效指标。

        参数:
            trades (List[Trade]): 成交记录列表。
            orders (List[Order]): 订单记录列表。
            account_history (List[AccountSnapshot]): 账户快照历史列表。

        返回:
            BacktestResult: 包含所有绩效指标的回测结果对象。
        """
        pass

    def calculate_returns(self, equity_curve: List[float]) -> ReturnMetrics:
        """
        计算收益指标。

        参数:
            equity_curve (List[float]): 权益曲线序列。

        返回:
            ReturnMetrics: 收益指标数据类。
        """
        pass

    def calculate_risk(
        self,
        returns: List[float],
        risk_free_rate: float
    ) -> RiskMetrics:
        """
        计算风险指标。

        参数:
            returns (List[float]): 收益率序列。
            risk_free_rate (float): 无风险利率。

        返回:
            RiskMetrics: 风险指标数据类。
        """
        pass

    def calculate_drawdown(self, equity_curve: List[float]) -> DrawdownMetrics:
        """
        计算回撤指标。

        参数:
            equity_curve (List[float]): 权益曲线序列。

        返回:
            DrawdownMetrics: 回撤指标数据类。
        """
        pass

    def calculate_sharpe(
        self,
        returns: List[float],
        risk_free_rate: float
    ) -> float:
        """
        计算夏普比率。

        参数:
            returns (List[float]): 收益率序列。
            risk_free_rate (float): 无风险利率。

        返回:
            float: 夏普比率。
        """
        pass

    def calculate_sortino(
        self,
        returns: List[float],
        risk_free_rate: float
    ) -> float:
        """
        计算索提诺比率。

        参数:
            returns (List[float]): 收益率序列。
            risk_free_rate (float): 无风险利率。

        返回:
            float: 索提诺比率。
        """
        pass

    def calculate_calmar(
        self,
        annual_return: float,
        max_drawdown: float
    ) -> float:
        """
        计算卡玛比率。

        参数:
            annual_return (float): 年化收益率。
            max_drawdown (float): 最大回撤。

        返回:
            float: 卡玛比率。
        """
        pass

    def calculate_profit_factor(
        self,
        gross_profit: float,
        gross_loss: float
    ) -> float:
        """
        计算盈亏比。

        参数:
            gross_profit (float): 总盈利。
            gross_loss (float): 总亏损。

        返回:
            float: 盈亏比。
        """
        pass

    def calculate_win_rate(self, trades: List[Trade]) -> float:
        """
        计算胜率。

        参数:
            trades (List[Trade]): 成交记录列表。

        返回:
            float: 胜率（0-1之间）。
        """
        pass

    def calculate_profit_loss_ratio(self, trades: List[Trade]) -> float:
        """
        计算盈亏比（笔均）。

        参数:
            trades (List[Trade]): 成交记录列表。

        返回:
            float: 笔均盈亏比。
        """
        pass

    def calculate_max_consecutive_losses(self, trades: List[Trade]) -> int:
        """
        计算最大连续亏损次数。

        参数:
            trades (List[Trade]): 成交记录列表。

        返回:
            int: 最大连续亏损次数。
        """
        pass

    def calculate_avg_hold_period(self, trades: List[Trade]) -> float:
        """
        计算平均持仓周期。

        参数:
            trades (List[Trade]): 成交记录列表。

        返回:
            float: 平均持仓天数。
        """
        pass

    def generate_atr_report(self, trades: List[Trade]) -> dict:
        """
        生成ATR报告。

        参数:
            trades (List[Trade]): 成交记录列表。

        返回:
            dict: ATR相关统计报告。
        """
        pass

    def generate_benchmark_comparison(
        self,
        portfolio_returns: List[float],
        benchmark_returns: List[float]
    ) -> dict:
        """
        基准对比。

        参数:
            portfolio_returns (List[float]): 组合收益率序列。
            benchmark_returns (List[float]): 基准收益率序列。

        返回:
            dict: 基准对比结果。
        """
        pass
```

#### 2.5.2 ReturnMetrics 数据类

定义收益指标数据类：

```python
@dataclass
class ReturnMetrics:
    """
    收益指标数据类。
    """
    total_return: float
    """float: 总收益率。"""

    annual_return: float
    """float: 年化收益率。"""

    daily_return_mean: float
    """float: 日收益率均值。"""

    daily_return_std: float
    """float: 日收益率标准差。"""

    cumulative_returns: List[float]
    """List[float]: 累计收益率序列。"""

    daily_returns: List[float]
    """List[float]: 日收益率序列。"""
```

#### 2.5.3 RiskMetrics 数据类

定义风险指标数据类：

```python
@dataclass
class RiskMetrics:
    """
    风险指标数据类。
    """
    sharpe_ratio: float
    """float: 夏普比率。"""

    sortino_ratio: float
    """float: 索提诺比率。"""

    calmar_ratio: float
    """float: 卡玛比率。"""

    information_ratio: float
    """float: 信息比率。"""

    tracking_error: float
    """float: 跟踪误差。"""

    beta: float
    """float: Beta值。"""

    alpha: float
    """float: Alpha值。"""

    volatility_annual: float
    """float: 年化波动率。"""

    value_at_risk_95: float
    """float: 95% VaR。"""

    value_at_risk_99: float
    """float: 99% VaR。"""

    conditional_var_95: float
    """float: 95% CVaR。"""
```

#### 2.5.4 DrawdownMetrics 数据类

定义回撤指标数据类：

```python
@dataclass
class DrawdownMetrics:
    """
    回撤指标数据类。
    """
    max_drawdown: float
    """float: 最大回撤。"""

    max_drawdown_duration: int
    """int: 最大回撤持续天数。"""

    current_drawdown: float
    """float: 当前回撤。"""

    drawdown_curve: List[float]
    """List[float]: 回撤曲线。"""

    drawdown_start: Optional[datetime] = None
    """Optional[datetime]: 最大回撤开始时间。"""

    drawdown_end: Optional[datetime] = None
    """Optional[datetime]: 最大回撤结束时间。"""

    drawdown_recovery: Optional[datetime] = None
    """Optional[datetime]: 回撤恢复时间。"""
```

### 2.6 收益归因模块

#### 2.6.1 AttributionAnalyzer 类

定义收益归因分析类：

```python
class AttributionAnalyzer:
    """
    收益归因分析器。
    """

    def brinson_attribution(
        self,
        portfolio_returns: List[float],
        benchmark_returns: List[float],
        sector_weights_portfolio: Dict[str, float],
        sector_weights_benchmark: Dict[str, float],
        sector_returns_portfolio: Dict[str, float],
        sector_returns_benchmark: Dict[str, float]
    ) -> BrinsonResult:
        """
        Brinson归因分析。

        参数:
            portfolio_returns: 组合收益率序列。
            benchmark_returns: 基准收益率序列。
            sector_weights_portfolio: 组合行业权重。
            sector_weights_benchmark: 基准行业权重。
            sector_returns_portfolio: 组合行业收益。
            sector_returns_benchmark: 基准行业收益。

        返回:
            BrinsonResult: Brinson归因结果。
        """
        pass

    def factor_attribution(
        self,
        portfolio_returns: List[float],
        factor_returns: Dict[str, List[float]]
    ) -> FactorAttributionResult:
        """
        多因子归因分析。

        参数:
            portfolio_returns: 组合收益率序列。
            factor_returns: 因子收益率字典。

        返回:
            FactorAttributionResult: 因子归因结果。
        """
        pass

    def strategy_attribution(
        self,
        strategy_results: Dict[str, List[float]]
    ) -> StrategyAttributionResult:
        """
        策略归因分析。

        参数:
            strategy_results: 各策略收益率字典。

        返回:
            StrategyAttributionResult: 策略归因结果。
        """
        pass

    def style_attribution(
        self,
        portfolio_returns: List[float],
        style_factors: Dict[str, List[float]]
    ) -> StyleAttributionResult:
        """
        风格归因分析。

        参数:
            portfolio_returns: 组合收益率序列。
            style_factors: 风格因子收益率字典。

        返回:
            StyleAttributionResult: 风格归因结果。
        """
        pass
```

#### 2.6.2 BrinsonResult 数据类

定义Brinson归因结果：

```python
@dataclass
class BrinsonResult:
    """
    Brinson归因结果数据类。
    """
    allocation_effect: float
    """float: 配置效应。"""

    selection_effect: float
    """float: 选股效应。"""

    interaction_effect: float
    """float: 交互效应。"""

    total_active_return: float
    """float: 总主动收益。"""

    sector_breakdown: List[SectorAttribution]
    """List[SectorAttribution]: 行业分解。"""
```

#### 2.6.3 FactorAttributionResult 数据类

定义因子归因结果：

```python
@dataclass
class FactorAttributionResult:
    """
    因子归因结果数据类。
    """
    factor_exposures: Dict[str, float]
    """Dict[str, float]: 因子暴露。"""

    factor_returns: Dict[str, float]
    """Dict[str, float]: 因子收益。"""

    specific_return: float
    """float: 特异性收益。"""

    r_squared: float
    """float: R²。"""

    factor_contribution: Dict[str, float]
    """Dict[str, float]: 因子贡献度。"""
```

#### 2.6.4 StrategyAttributionResult 数据类

定义策略归因结果：

```python
@dataclass
class StrategyAttributionResult:
    """
    策略归因结果数据类。
    """
    strategy_contributions: Dict[str, float]
    """Dict[str, float]: 各策略贡献度。"""

    correlation_matrix: Dict[str, Dict[str, float]]
    """Dict[str, Dict[str, float]]: 策略相关性矩阵。"""

    diversification_ratio: float
    """float: 分散化比率。"""

    marginal_contribution: Dict[str, float]
    """Dict[str, float]: 边际贡献。"""
```

#### 2.6.5 StyleAttributionResult 数据类

定义风格归因结果：

```python
@dataclass
class StyleAttributionResult:
    """
    风格归因结果数据类。
    """
    style_exposures: Dict[str, float]
    """Dict[str, float]: 风格暴露。"""

    style_returns: Dict[str, float]
    """Dict[str, float]: 风格收益。"""

    net_style_return: float
    """float: 净风格收益。"""

    residual_return: float
    """float: 残差收益。"""
```

### 2.7 数据验证与防泄露

#### 2.7.1 DataIntegrityChecker 类

定义数据完整性检查类：

```python
class DataIntegrityChecker:
    """
    数据完整性检查器。
    """

    def check_future_leakage(
        self,
        strategy: Strategy,
        datafeed: DataFeed
    ) -> LeakageReport:
        """
        检查未来函数泄露。

        参数:
            strategy: 策略实例。
            datafeed: 数据源实例。

        返回:
            LeakageReport: 泄露检测报告。
        """
        pass

    def check_survivorship_bias(self, datafeed: DataFeed) -> BiasReport:
        """
        检查幸存者偏差。

        参数:
            datafeed: 数据源实例。

        返回:
            BiasReport: 偏差检测报告。
        """
        pass

    def check_data_completeness(
        self,
        symbol: str,
        start: datetime,
        end: datetime
    ) -> CompletenessReport:
        """
        检查数据完整性。

        参数:
            symbol: 标的代码。
            start: 开始时间。
            end: 结束时间。

        返回:
            CompletenessReport: 完整性检测报告。
        """
        pass

    def check_point_in_time(
        self,
        symbol: str,
        date: datetime,
        field: str
    ) -> PITReport:
        """
        检查Point-in-Time一致性。

        参数:
            symbol: 标的代码。
            date: 时间点。
            field: 字段名。

        返回:
            PITReport: PIT检查报告。
        """
        pass

    def check_lookahead_bias(
        self,
        strategy: Strategy,
        datafeed: DataFeed
    ) -> BiasReport:
        """
        检查前视偏差。

        参数:
            strategy: 策略实例。
            datafeed: 数据源实例。

        返回:
            BiasReport: 偏差检测报告。
        """
        pass

    def run_full_validation(self) -> ValidationSuiteResult:
        """
        运行完整验证套件。

        返回:
            ValidationSuiteResult: 完整验证套件结果。
        """
        pass
```

#### 2.7.2 LeakageReport 数据类

定义泄露检测报告：

```python
@dataclass
class LeakageReport:
    """
    泄露检测报告数据类。
    """
    has_leakage: bool
    """bool: 是否存在泄露。"""

    leakage_points: List[LeakagePoint]
    """List[LeakagePoint]: 泄露点列表。"""

    severity: str
    """str: 严重程度（CRITICAL/WARNING/INFO）。"""

    description: str
    """str: 描述。"""

    recommendation: str
    """str: 建议。"""
```

#### 2.7.3 BiasReport 数据类

定义偏差检测报告：

```python
@dataclass
class BiasReport:
    """
    偏差检测报告数据类。
    """
    has_bias: bool
    """bool: 是否存在偏差。"""

    bias_type: str
    """str: 偏差类型。"""

    bias_magnitude: float
    """float: 偏差幅度。"""

    affected_periods: List[str]
    """List[str]: 受影响时段。"""

    recommendation: str
    """str: 建议。"""
```

#### 2.7.4 CompletenessReport 数据类

定义完整性检测报告：

```python
@dataclass
class CompletenessReport:
    """
    完整性检测报告数据类。
    """
    is_complete: bool
    """bool: 是否完整。"""

    missing_dates: List[datetime]
    """List[datetime]: 缺失日期。"""

    missing_bars: List[str]
    """List[str]: 缺失K线。"""

    anomaly_flags: List[str]
    """List[str]: 异常标记。"""

    completeness_rate: float
    """float: 完整率。"""
```

#### 2.7.5 ValidationSuiteResult 数据类

定义完整验证套件结果：

```python
@dataclass
class ValidationSuiteResult:
    """
    完整验证套件结果数据类。
    """
    overall_status: str
    """str: 总体状态（PASS/FAIL/WARNING）。"""

    checks_run: int
    """int: 执行的检查数。"""

    checks_passed: int
    """int: 通过的检查数。"""

    checks_failed: int
    """int: 失败的检查数。"""

    checks_warning: int
    """int: 警告数。"""

    detailed_reports: List
    """List: 详细报告列表。"""

    summary: str
    """str: 汇总描述。"""
```

### 2.8 回测结果序列化

#### 2.8.1 ResultSerializer 类

定义回测结果序列化类：

```python
class ResultSerializer: """ 回测结果序列化器。 """
def to_json(self, result: BacktestResult, path: str) -> bool:
    """
    导出为JSON。

    参数:
        result: 回测结果对象。
        path: 输出文件路径。

    返回:
        bool: 是否成功。
    """
    pass

def from_json(self, path: str) -> BacktestResult:
    """
    从JSON导入。

    参数:
        path: JSON文件路径。

    返回:
        Backtest
```

### 2.9 回测引擎使用示例

以下是一个完整的 Python 使用示例代码，演示了从配置、数据加载、策略注册到执行回测及结果分析的完整流程。假设底层依赖已就绪：

```python
import pandas as pd
from backtest_engine import (
    BacktestConfig,
    CsvDataFeed,
    MA_Cross_Strategy,
    BacktestEngine,
    DataIntegrityChecker,
    ResultSerializer
)

def run_full_backtest():
    # 1. 配置回测参数
    config = BacktestConfig(
        start_date="2023-01-01",
        end_date="2023-12-31",
        initial_capital=1000000,
        commission_rate=0.0003,
        slippage=0.002
    )

    # 2. 加载CSV数据源
    data_feed = CsvDataFeed(
        file_path="./data/historical_prices.csv",
        date_col="trade_date",
        symbol_col="ticker"
    )

    # 3. 创建并注册均线交叉策略
    strategy = MA_Cross_Strategy(
        short_window=10,
        long_window=30
    )

    # 4. 执行回测
    engine = BacktestEngine(config=config, data_feed=data_feed)
    engine.register_strategy(strategy)
    backtest_result = engine.run()

    # 5. 获取和分析结果
    performance = backtest_result.get_performance()
    print(f"总收益率: {performance.total_return:.2%}")
    print(f"最大回撤: {performance.max_drawdown:.2%}")
    print(f"夏普比率: {performance.sharpe_ratio:.2f}")

    # 6. 运行数据完整性验证
    checker = DataIntegrityChecker(data_feed=data_feed, strategy=strategy)
    validation_report = checker.validate()
    if not validation_report.is_valid:
        print("数据验证警告:", validation_report.warnings)

    # 7. 导出回测报告
    serializer = ResultSerializer(backtest_result)
    serializer.export_to_csv("./output/backtest_report.csv")
    serializer.export_summary("./output/backtest_summary.json")

if __name__ == "__main__":
    run_full_backtest()
```

## 附录

### A. 版本变更记录

版本: v1.0 日期: 2026-09-23 作者: 架构组 变更内容: 初始版本，定义策略中心和回测引擎核心接口

### B. 依赖关系说明

模块依赖关系如下： - 策略中心（Strategy Center）依赖：事件引擎（EventEngine）、数据源抽象（DataFeed） - 回测引擎（Backtesting Engine）依赖：策略中心、事件引擎、模拟撮合引擎（SimulatedBroker）、绩效分析器（PerformanceAnalyzer） - 绩效分析器（PerformanceAnalyzer）依赖：回测引擎（消费回测结果数据） - 收益归因器（AttributionAnalyzer）依赖：绩效分析器（消费绩效数据） - 数据验证器（DataIntegrityChecker）依赖：回测引擎（消费数据源和策略） - 结果序列化器（ResultSerializer）依赖：回测引擎（消费回测结果）

### C. 测试要求

模块: 策略基类 测试场景: 初始化测试、启动/停止测试、数据处理测试、参数更新测试 覆盖要求: 必须

模块: 策略注册表 测试场景: 注册/注销测试、状态管理测试、并发安全测试 覆盖要求: 必须

模块: 适配引擎 测试场景: 适配度计算测试、股票池管理测试、市场状态更新测试 覆盖要求: 必须

模块: 回测引擎 测试场景: 完整回测测试、部分回测测试、检查点测试、并发回测测试 覆盖要求: 必须

模块: 数据源 测试场景: 数据加载测试、停牌处理测试、复权处理测试 覆盖要求: 必须

模块: 事件引擎 测试场景: 事件订阅/发布测试、事件顺序测试、并发安全测试 覆盖要求: 必须

模块: 撮合引擎 测试场景: 订单撮合测试、佣金计算测试、滑点模拟测试、资金校验测试 覆盖要求: 必须

模块: 绩效分析 测试场景: 指标计算准确性测试、边界条件测试 覆盖要求: 必须

模块: 数据验证 测试场景: 未来函数检测测试、幸存者偏差检测测试、数据完整性测试 覆盖要求: 必须

### D. 错误码完整列表

| 错误码 | 级别 | 描述 | 建议措施 |
| --- | --- | --- | --- |
| STRATEGY_001 | ERROR | 策略ID重复 | 检查策略注册逻辑，确保ID唯一性 |
| STRATEGY_002 | ERROR | 策略未找到 | 检查策略ID是否正确 |
| STRATEGY_003 | ERROR | 策略状态非法 | 检查策略当前状态是否允许该操作 |
| STRATEGY_004 | ERROR | 配置参数无效 | 检查配置参数类型和范围 |
| STRATEGY_005 | ERROR | 数据验证失败 | 检查输入数据格式和完整性 |
| STRATEGY_006 | ERROR | 策略执行异常 | 检查策略逻辑和数据处理代码 |
| STRATEGY_007 | ERROR | 适配度计算失败 | 检查适配度模型和输入数据 |
| STRATEGY_008 | ERROR | 市场状态获取失败 | 检查数据源连接和数据完整性 |
| STRATEGY_009 | ERROR | 策略注册失败 | 检查策略实例和配置 |
| STRATEGY_010 | ERROR | 策略注销失败 | 检查策略状态和依赖引用 |
| BACKTEST_001 | ERROR | 回测执行失败 | 检查回测配置和数据源 |
| BACKTEST_002 | ERROR | 数据源注册失败 | 检查数据源实现和路径 |
| BACKTEST_003 | ERROR | 检查点保存失败 | 检查磁盘空间和路径权限 |
| BACKTEST_004 | ERROR | 检查点恢复失败 | 检查检查点文件完整性 |
| BACKTEST_005 | WARNING | 数据不完整 | 检查数据源覆盖范围 |
| BACKTEST_006 | WARNING | 可能存在未来函数 | 审查策略数据访问逻辑 |
| BROKER_001 | ERROR | 订单提交失败 | 检查订单参数和资金充足性 |
| BROKER_002 | ERROR | 撮合失败 | 检查价格和数量参数 |
| BROKER_003 | WARNING | 滑点超出预期 | 检查滑点配置和市场流动性 |
| RISK_001 | CRITICAL | 风控拦截 | 检查风控规则和仓位管理 |
| RISK_002 | CRITICAL | 全局熔断触发 | 检查总回撤是否超过阈值 |
