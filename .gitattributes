# 禁止任何 EOL / 编码转换。
#
# 理由：本仓库的许多判据直接读文件字节 —— 例如 `tools/run_all_gates.py` 解析
# `tools/gates-report.txt` 里的 `FINDING [Tx]` 行数得到棘轮数字；`tools/run_sql_smoke.py`
# 写出的报告是可复核的证据快照。一旦 git 在提交或检出时改写行尾，行数与指纹就会漂移，
# 「同一份报告」在不同机器上不再是同一份字节。
#
# 所以：提交什么字节，检出就是什么字节。
* -text
